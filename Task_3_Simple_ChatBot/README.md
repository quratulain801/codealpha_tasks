🧠 Project Scope

The chatbot:

Takes user input like:

"hello"

"how are you"

"bye"

Responds with predefined replies such as:

"Hi!"

"I'm fine, thanks!"

"Goodbye!"

Runs continuously until the user chooses to exit.

🛠️ Key Concepts Used

if-elif-else (decision making)

functions

while loop

input() and print() (input/output)

String handling (lower())

🧪 How It Works

The program asks the user for input.

The input is checked using if-elif conditions.

A predefined response is printed.

The chatbot keeps running until the user types "bye"
